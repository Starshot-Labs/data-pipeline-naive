node_modules/
*.log
.DS_Store
__pycache__/

# Bulk content — samples, meshes, renders, experiment output. All local except the benchmark
# inputs both placement experiments read, which are small enough to travel with the repo.
data/*
!data/placement-set/
!data/models/
data/models/*
!data/models/.gitkeep

# Client build output
web/dist/

.env

# Mesh-stage working files, both self-cleaning
.trellis-campaign.json
.trellis-stage/
